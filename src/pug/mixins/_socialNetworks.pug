mixin socialNetworks(name, img, svg)

   a(href="#" class='profiles__social')
      .profiles__network
         span
            object(width='100%' data="./img/" + svg , type="image/svg+xml" class='svg')
               img(src="./img/" + img , alt=name)